# Local-only model bundles (large binary FBX/TGA assets, ~9 GB).
# Anchored to root so proto/assets/models/ (curated GLB+PNG output) stays tracked.
/models/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Node
node_modules/
npm-debug.log
